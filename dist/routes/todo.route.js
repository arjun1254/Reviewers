"use strict";
// /* eslint-disable @typescript-eslint/no-unsafe-argument */
// import express from 'express'
//import { getAll } from '../controllers/todo.controller'
//import * as TodoController from '../controllers/todo.controller'
// import { createtodoDto } from '../Validators/create-todo.Validators'
// import { validate } from '../utils/Validate'
// import { authenticateToken, isAdmin } from '../middleware/authentication.middleware'
// const route = express.Router()
// route.get('/', TodoController.get)
// route.post('/',validate(createtodoDto),authenticateToken,TodoController.create)
// route.delete('/:id',authenticateToken,TodoController.remove)
// route.put('/:id', TodoController.update)
// route.get('/:id',TodoController.findPostById)
// route.delete(`/:id`, authenticateToken, TodoController.deleteById)
//route.get(`/:id`, authenticateToken,  TodoController.updateByID)
// export default route;
//# sourceMappingURL=todo.route.js.map